<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="PZ5Shop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="����" Height="320" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="{StaticResource AppBackgroundBrush}">
    <Grid Margin="20">
        <Border Background="{StaticResource CardBrush}" CornerRadius="16" Padding="20" Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Text="���� � �������" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" />
                <StackPanel Grid.Row="1" Margin="0,12,0,0">
                    <TextBlock Text="�����" Foreground="{StaticResource SecondaryTextBrush}" />
                    <TextBox x:Name="UsernameBox" Margin="0,4,0,0" />
                </StackPanel>
                <StackPanel Grid.Row="2" Margin="0,12,0,0">
                    <TextBlock Text="������" Foreground="{StaticResource SecondaryTextBrush}" />
                    <PasswordBox x:Name="PasswordBox" Margin="0,4,0,0" />
                </StackPanel>
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="������" Width="110" Margin="0,0,8,0" Click="OnCancel" />
                    <Button Content="�����" Width="110" Click="OnLogin" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
